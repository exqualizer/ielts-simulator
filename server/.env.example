PORT=3001
NODE_ENV=development
MONGODB_URI=mongodb+srv://nicejames1818_db_user:tm4oHtyAQmkIF6Bt@exq-cluster.0kebf6b.mongodb.net/ielts-simulator?appName=exq-cluster
JWT_SECRET=change-me
COOKIE_NAME=access_token
# Comma-separated browser origins (required with credentials: true — no "*").
# When the API runs on Cloud Run, include every frontend origin, e.g.:
# CORS_ORIGIN=http://localhost:5173,https://YOURUSER.github.io
# (The Origin header is scheme+host only, e.g. GitHub Pages project sites still send https://YOURUSER.github.io.)
CORS_ORIGIN=http://localhost:5173

