# Optional: set when frontend calls a separately hosted backend.
# If empty, the app uses relative /api (works with Vite proxy in dev).
VITE_API_BASE_URL=https://ielts-simulator-api-887393880271.australia-southeast1.run.app

